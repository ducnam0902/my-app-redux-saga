import React, { ReactElement } from 'react'

interface PrivateRouteProps {
    
}

export default function PrivateRoute({}: PrivateRouteProps): ReactElement {
    return (
        <div>
            
        </div>
    )
}
